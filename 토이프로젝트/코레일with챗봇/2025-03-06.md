# 코레일 with 챗봇

## 2일차
docker를 공부하기로 했다. docker는 여러 서버를 사용하라고 해서 사용했지 그게 무슨 원리로 돌아가고 어떻게 구성해야 하는지를 알지도 못하고 구현했기 때문에 공부하기로 했다.

docker 공부 참고 사이트

https://pyrasis.com/jHLsAlwaysUpToDateDocker

위에 있는 사이트는 챕터별로 공부할 거리와 실습할 코드를 제공하여 docker의 흐름을 알 수 있었다. 챕터가 많아 이것을 하루종일 공부하여 2일차는 docker공부하느라 시간을 보냈다.


## 3일차
3일차에는 이제 챗봇을 쓰기 위해서는 LLM을 공부해야 해서 LLM이 어떤 기능을 가지고 있는지 어떻게 활용해야 하는지를 공부를 했었다. 그래서 "랭체인으로 실현하는 LLM 아키텍처"이라는 책을 읽었다. 그래서 LLM은 어떤 기능이 있고 어떤 모델을 써야하는지 공부했다.

## 4일차
4일차에는 Nginx의 기능과 jwt의 구성요소를 공부했다. 스프링 시큐리티도 자세히 공부한 적이 없어서 스프링 공식문서를 참고해서 공부를 했다. 공부를 다 마치고 본격적인 코딩을 시작헀다. 먼저 Secuity Config를 설정했다. 맨처음 FilterChain을 설정해야 했다.

기본적인 HTTP Basic설정하고 CSRF 비활성화를 했다. 그리고 JWT를 쓰기 때문에 세션 정책은 STATELESS로 설정해 jwt를 로컬 스토리지에 설치하기로 했다.

그다음 요청별 접근 제어를 해 인증관련 경로는 모두 허용하고 다른 것은 인증하기로 했다. 그다음 JWT 인증 필터를 등록함으로써 설정을 마쳤다.

그다음 JWT 인증필터를 설정해 요청이 들어오면 Bearer 토큰을 추출하고 토큰에서 email을 추출하고 인증 객체를 생성하고 정보를 설정을 했다.

여기까지 하고 4일차는 마무리 했다.